% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/VisModelSpace.R
\name{VisModelSpace}
\alias{VisModelSpace}
\title{Visualize Model Space}
\usage{
VisModelSpace(data, cex.u = 3, cex.mtext = 1, cex.leg = 0.8, cex.mod = 0.35)
}
\arguments{
\item{data}{A \code{"genarch"} object returned by \code{\link[=LCA]{LCA()}}.}

\item{cex.u}{Numeric. Expansion factor for the model squares. Default
is \code{3}.}

\item{cex.mtext}{Numeric. Expansion factor for axis labels. Default
is \code{1}.}

\item{cex.leg}{Numeric. Expansion factor for the legend text. Default
is \code{0.8}.}

\item{cex.mod}{Numeric. Expansion factor for model number labels within
each square. Default is \code{0.35}.}
}
\value{
Called for its side effect of producing a plot. Returns \code{NULL}
invisibly.
}
\description{
Plots a grid of colored squares representing every model evaluated during
line cross analysis. The color of each square reflects its Akaike weight,
with hotter colors indicating higher support. Models are arranged from
simplest (bottom-left) to most complex (top-right).
}
\examples{
data(BanInf)
result <- LCA(BanInf, SCS = "NSC", messages = FALSE)
VisModelSpace(result)
}
\seealso{
\code{\link[=LCA]{LCA()}} for performing the analysis, \code{\link[=EvaluateModel]{EvaluateModel()}} for
investigating individual models.
}
