% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.genarch.R
\name{plot.genarch}
\alias{plot.genarch}
\title{Plot Model-Averaged Line Cross Analysis Results}
\usage{
\method{plot}{genarch}(
  x,
  min.vi = 0.5,
  main = NULL,
  cex.axis = 1,
  cex.names = 1,
  cex.main = 1,
  maxval = NULL,
  minval = NULL,
  col.ramp = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{"genarch"} object returned by \code{\link[=LCA]{LCA()}}.}

\item{min.vi}{Numeric between 0 and 1. Minimum variable importance
threshold for including a CGE in the plot. Default is \code{0.5}.}

\item{main}{Character. Main title for the plot. Default is
\code{"Model Weighted Averages and Unconditional SE"}.}

\item{cex.axis}{Numeric. Expansion factor for the Y-axis labels. Default
is \code{1}.}

\item{cex.names}{Numeric. Expansion factor for the CGE names on the
X-axis. Default is \code{1}.}

\item{cex.main}{Numeric. Expansion factor for the main title. Default
is \code{1}.}

\item{maxval}{Numeric. Custom maximum value for the Y-axis. If \code{NULL},
determined automatically from the data. Default is \code{NULL}.}

\item{minval}{Numeric. Custom minimum value for the Y-axis. If \code{NULL},
determined automatically from the data. Default is \code{NULL}.}

\item{col.ramp}{A vector of colors to use for the variable importance
color scale. Default is \code{heat.colors(100)[100:1]}.}

\item{...}{Additional arguments passed to \code{\link[=barplot]{barplot()}}.}
}
\value{
Called for its side effect of producing a plot. Returns \code{NULL}
invisibly.
}
\description{
S3 plot method for \code{"genarch"} objects produced by \code{\link[=LCA]{LCA()}}. Creates a
barplot of model-weighted average parameter estimates with unconditional
standard error bars. Bars are colored by variable importance, with hotter
colors indicating higher importance. Only composite genetic effects (CGEs)
with variable importance at or above \code{min.vi} are included.
}
\examples{
data(BanInf)
result <- LCA(BanInf, SCS = "NSC", messages = FALSE)
plot(result)
plot(result, min.vi = 0.3)
}
\seealso{
\code{\link[=LCA]{LCA()}} for performing the analysis, \code{\link[=VisModelSpace]{VisModelSpace()}} for
model space visualization, \code{\link[=EvaluateModel]{EvaluateModel()}} for single model plots.
}
